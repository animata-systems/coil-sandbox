/**
 * Agent viewer entry — STUB (Group A).
 *
 * Phase 5 Group A establishes the build toolchain. This stub only imports the
 * Tailwind CSS so the build produces both agent-viewer.js and agent-viewer.css,
 * and logs a sanity message on module load. The real React bootstrap and
 * window.openAgentViewer registration land in Group B (task 5.3, 5.4).
 */

import './viewer.css';

// eslint-disable-next-line no-console
console.log('[coil-sandbox] agent-viewer stub loaded');
